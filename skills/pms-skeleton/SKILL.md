---
name: pms-skeleton
description: >-
  Name what this actually is — the core, not the costume. Use when language is describing events, vibes, or branding instead of the structure. Invoke with /pms-skeleton or /skeleton.
when-to-use: Language is describing events, vibes, or branding instead of the structure. Also: /pms-skeleton, /skeleton, name the core.
argument-hint: optional: the structure to name
user-invocable: true
metadata:
  author: Junction AI
  short-description: "Name what this actually is — the core, not the costume."
  phase: understand
---

# Name the core

/pms-skeleton — Name what this actually is — the core, not the costume.

Phase: **02 Understand**. See what drives it.

Also invoked as `/skeleton`.

## When
Language is describing events, vibes, or branding instead of the structure.

## Reads
Mapped frame and evidence.

## Writes
A core statement: governing function, not a slogan.

## Steps
1. Strip costume: events, branding, mood.
2. State governing function in one or two sentences.
3. If not established, say so.

## Will not do
- Will not produce a slogan, mission statement, or identity story.

## Output
## Skeleton
- core:
- governing_function:
- costume_rejected: []

## Example
`/pms-skeleton what is this job search actually a structure of`

## Doctrine
Observe. Understand. Navigate. Align.

- Provenance: reported | observed | inferred | not established.
- Do not invent progress, probability, or completion percentages.
- Empty is allowed. Gaps stay gaps.
- Forward/drag are relative to a named criterion or destination.
- If live context is not actually available, mark it **missing**.
